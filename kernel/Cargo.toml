[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
limine = "0.3.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[[bin]]
name = "kernel"
test = false
bench = false
